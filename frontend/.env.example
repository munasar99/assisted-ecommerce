# Development (Vite proxy → http://localhost:5298)
VITE_API_URL=/api

# Production (Vercel): hal domain — /api waxaa loo wareejiyaa Render (vercel.json rewrites)
# Vercel → Environment: VITE_API_URL=/api
# Beddel vercel.json "destination" haddii API URL Render ka duwan yahay

# Haddii proxy aan shaqayn: backend toos
# VITE_API_URL=https://assisted-ecommerce-api.onrender.com/api
